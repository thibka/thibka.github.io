/** A constructor option documented in the "Configure the panel" grid. */
export interface GuiOption {
  name: string;
  type: string;
  desc: string;
}

/** A row of a method's parameter table. */
export interface MethodParam {
  name: string;
  type: string;
  desc: string;
}

